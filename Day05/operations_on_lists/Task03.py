liste1=list(map(lambda x: x * x, [3, 2, 6, 7, 1, 4]))

print(liste1)

# each element of teh list is multiplied by himself
# map applies a function on each element of the list
# map takes 2 arguments: the function and the list
# lambda is a function which makes an operation defined
# we convert the result (object map) to a list with list()